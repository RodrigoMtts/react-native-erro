export default class Terefa {
    constructor(nome, descricao, dataTermino, prioridade) {
        this.Nome = nome;
        this.Descricao = descricao;
        this.DataTermino = dataTermino;
        this.Prioridade = prioridade;
    }
}